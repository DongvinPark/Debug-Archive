# 첫 SI 프로젝트에서 '기술'에 대해 배운 점
### 작성일 : 2026 September 04

## 들어가며

23년 5월에 입사한 후 자사 앱의 백엔드를 TypeScript에서 Java/Spring 으로 전환하는 작업을 했었다.

그 후엔 ExoPlayer, VR Player, RTSP Streaming Server, ExpPlayer RTSP part tuning, AI test(CUDA, CuDNN, TensorFlow,...) 등 다른 작업을 하느라 API 서버쪽은 거의 손대지 않고 있었다.

그러다가 거의 3년 만인 26년 3월 말 경에 팬덤 채팅 앱의 마이그레이션 & 재개발 프로젝트의 유일한 서버 개발자로 투입되면서 백엔드 전체를 책임지게 되었다.

최초 개발 시작 ~ 상용 배포/디버깅/고객사 후임 개발자에게 인수인계까지 5 개월의 시간이 걸렸고,
- Firebase Cloud Messaging
- Redis Pub & Sub
- Spring STOMP & WebSocket
- Toss Billing Payments API
- Shedlock Scheduler and Redisson Lock for Distributed Lock
- AWS Secrets Manager

등을 처음 다뤄보면서 기술적 경험을 넓힐 수 있었다. 프로젝트 완료 후 팬덤 채팅앱의 총 유저 수는 약 5.5K 정도였다.

---

## 1. 성격이 다른 서버는 인스턴스를 분리해야 한다.

레거시 서버의 아키텍처는 아래와 같다.

<img width="555" height="484" alt="Image" src="https://github.com/user-attachments/assets/e5c928ab-746d-4e38-8b30-120cc26e4ce1" />

- Django 서버가 API 서버의 역할과 챗 서버의 역할을 모두 책임진다.

- API 서버는 State less 서버이고, 실시간 챗 전송을 담당하는 Chat 서버는 개별 웹소켓 연결 자체가 하나의 상태이므로 Stateful 한 서버이다.

- 성격이 서로 다른 서버가 하나의 인스턴스 안에 있다보니 여러 문제점이 있었다.
    - API 서버가 죽거나 새로 배포되면 웹소켓도 전부 연결이 유실된다.
    - API 서버 로직과 Chat 서버 로직이 서로의 성능을 깎아 먹는다.
    - K8S 컨테이너 환경이라서 Chat 서버를 위한 전용 설정(커널 & 소켓 관련 튜닝)을 할 수 없다.

따라서 나는 아래와 같이 API 서버와 Chat 서버를 완전히 다른 종류의 인스턴스로 분리 한 후, Chat 서버는 오직 'Redis pub/sub 채널 리스닝 후 적절한 웹소켓으로 실시간 챗 전달'의 역할만 담당하게 만들었다.

<img width="810" height="497" alt="Image" src="https://github.com/user-attachments/assets/0e947529-0be5-498a-8e39-76e8094f2df5" />

- 이렇게 인스턴스를 분리함으로써 다음과 같은 이점을 얻을 수 있었다.
    - API 서버가 언제 어떻게 배포/교체 되도 웹소켓 연결은 영향을 받지 않는다.
    - API 서버와 Chat 서버가 서로의 성능을 깎아 먹을 일이 없다.
    - Chat 서버는 EC2 에서 실행되기 때문에 원하는대로 커널 & 소켓 관련 튜닝을 할 수 있다.

---

## 2. '정상적인 상황'을 기대하면 안 된다. 현실이 생각보다 더 황당하기 때문이다.

다음은 레거시 서버에서 실제로 일어난(그래서 내가 전부 발견/조치/인수인계 한) 일들이다.

- FCM 알림 전송을 전부 Sync/Blocking 으로 전송한다고??
    - 새 서버에서는 JDK virtual thread와 @Async 를 적극 활용한다.

- DB 내 CASCADE 옵션을 켜 놓은 상태로 FK 상 최상위 테이블 내의 로우를 물리적으로 삭제한다고??
    - CASCADE 옵션을 당장 전부 비활성화 하고, 모든 테이블 내의 모든 로우들에 대힌 물리적인 삭제를 일절 금지함.

- 핵심 비즈니스 규칙을 담당하는 테이블에 DB 레벨의 제약조건이 없다고??
    - 레거시 서버가 상용으로 돌아가고 있는 상태에서 UNIQUE 제약 조건들을 핫픽스로 적용시킴.

- 실수로 물리적 삭제를 해버린 팬들의 메시지들을 복구한답시고 DB를 더 엉망으로 만들어??
    - 엉망이 된 테이블을 전수조사하여 '진짜 복구 대상 데이터'와 '복구할 필요가 없는 데이터'를 구별하는 방법을 찾아내서 정리하고 인수인계 함.

- 한 번 구독해지처리한 멤버십을 다음 날도, 그 다음 날도 계속 다시 구독 해지를 해??
    - 새 서버에서는 해당 테이블을 전혀 사용하지 않으며, 인수인계 때 주의사항으로 전달함.

---

## 3. Cache 레이어의 진짜 비용은 구현의 어려움이 아니라 테스트 횟수다.

팬덤 채팅앱에서 가장 자주 호출되는 API는 채팅룸 리스트 GET API와 각 채팅룸별 최신 메시지 리스트 GET API다.

리턴 레거시 서버에서는 이 두 가지 API를 항상 DB에서만 읽어 왔고, 프로젝트 초반에 고객사 PM과 대화했을 때 '팬들이 채팅룸 입장할 때 메시지가 늦게 뜯다는 컴플레인을 많이 합니다'라는 이야기를 들었었다.

새 서버에서는 이 두 가지 API에 대해서 DB의 부하를 줄이고 채팅 룸 입장시 챗들이 더 빨리 뜨게 만들기 위해서 Redis를 적극 활용했다.

유저 수 5.5K 면 많은 편은 아니지만 그럼에도 불구하고 '속도' 관련 컴플레인이 들어오고 있었고, 매주 유저수가 100 ~ 200 명씩 늘어나고 있었기 때문에 DB의 부하를 줄이고자 해당 API 들에 대한 Redis Cache layer 도입을 결정했다.

**그 후에는 그야말로 테스트 지옥이 펼쳐졌다.**

고객사 PM으로부터 "채팅룸 리스트 / 채팅룸 내 메시지 리스트가 이상한데요?" 라는 이슈가 10 번은 넘게 만들어졌고, 그 이슈의 원인은 대부분 DB와 Redis 내의 데이터 불일치/동기화 실패였던 것이다. 관련 디버깅을 함에 있어서 엄격하게 수없이 테스트를 해준 고객사 PM의 역할이 매우 중요함도 느낄 수 있었다.

결국 거의 모든 실패들을 다 바로잡고나니 '채팅룸 리스트 GET API'와 '채팅 룸 내 최신 메시지 리스트 GET API'는 각각 300 줄을 넘어가는 복잡한(그리고 많은 책임도 떠안은) 로직이 됐다.

한 가지 그나마 다행인 점은 새 서버 & 새 앱으로 상용 배포를 한 후 실시간으로 팬들과 아티스트들이 '대화하기 편해졌다', '접속이 빨라졌다'라는 대화를 나누는걸 봤다는 점이다.

누군가가 '저는 캐시를 써서 DB의 부하를 줄여봤습니다'라고 나에게 말한다면, 나는 아래의 두 가지를 꼭 물어보고 싶다.
- 테스트를 얼마나 많이 했는지?
- 테스트 과정에서 PM 혹은 고객사 담당자들과 적극적으로/원만하게 협력했는지?

---

## 4. 작은 규모의 서비스에서도 분산 시스템 문제는 발생한다.

새 서버에서는 분산 시스템 문제와 관련하여 '캐시' 문제를 제외하고는 2 가지의 조치를 취하였다.

1. shedlock 테이블 & 관련 라이브러리를 사용하여 서버 컨테이너가 여러 개 들고 있어도 특정 스케줄러(특히 토스 빌링 결제 스케줄러)는 반드시 하나의 서버 컨테이너에서만 실행되게 했다.
    - 여러 서버 컨테이너가 모두 토스 빌링 결제 스케줄러를 돌린다면 '중복결제' 사고가 터지는 것이다.

2. Redisson Distributed Lock을 사용하여 동일 결제 요청이 매우 짧은 간격으로 서버에 도달해도 가장 먼저 도달한 요청 하나만 처리 성공하게 만들었다.

---

## 5. 데이터는 변화한다. 그래서 로직도 영향을 받지만 이것을 처음부터 예견하는 건 매우 어렵다.

이것은 새 서버에서 '채팅룸 리스트 GET API'와 '채팅룸 내 최신 메시지 리스트 GET API'를 개발하면서 깨닫게 된 것이다.

최초 로직은 로컬 환경과 QA 환경에서 모두 정상 작동했다.

문제는 상용 배포를 하고나서 터졌다.

로컬 / QA 환경에서는 정적인 DB에서 테스트를 했지만, 상용환경에선느 실시간으로 DB를 변화시키는 수많은 다른 유저들이 존재하기 때문이다.

최신 메시지를 잡아내는 쿼리가 local/QA 환경에서는 '전체 팬들에게 보내진 메시지'와 '개별 팬에게만 전송된 메시지'를 구별하지 않았지만, 상용 환경에서는 이룰 구별하지 않을 경우 '특정 팬에게 전송된 메시지'가 다른 팬들에게도 보였던 것이다.

결국 상용 배포를 한 후에도 두 API에 들어가는 핵심 쿼리들을 수정해서 라이브로 핫 픽스를 했다.

이러한 '데이터의 변경'에 의한 실패는 최초에 API를 내가 개발할 당시에는 예견하는 것이 사실상 불가능했다. 내 경험이 부족한 것도 있지만 '현재 로직/구현의 근거가 되는 이 데이터가 미래엔 어떻게 변화할 것인가?'를 완벽하게 예측하는건 인간의 능력 밖이기 때문이다.

결국 경험을 통해서 배우는 수밖에 없는 듯 하다.

앞으로는 '로직의 근거가 되는 데이터'가 시간이 흐름에 따라 변할 수 있고, 그것 때문의 지금의 로직이 상용 환경에서는 실패할 수 있음을 개발 초기부터 인지하려 한다.

---

## 결론

이번 프로젝트는 개발, 테스트, QA 배포, PRD 배포, 운영 중 Hot Fix, 인수인계까지 전부 내 손으로 마친 최초의 프로젝트라는 점에서 나의 커리어 상 의미가 깊다.

이번 프로젝트를 시작하기 전까지 나는 백엔드 개발을 주로 "요구사항을 코드로 구현하는 일"​이라고 생각했다.

하지만 약 5개월 동안 하나의 서비스를 처음부터 끝까지 책임져 보면서, 실제 운영되는 서버를 만드는 일은 단순히 코드를 작성하는 것과는 상당히 다르다는 것을 배웠다.

개발 환경에서는 정상적으로 동작하던 코드가 상용 환경에서는 실패할 수 있고, 예상하지 못한 데이터가 들어올 수 있으며, 여러 서버가 동시에 같은 작업을 수행할 수도 있다.

또한 Redis Cache를 도입하면 단순히 DB의 부하를 줄이는 것으로 끝나는 것이 아니라 데이터 정합성이라는 새로운 책임이 생기고, Scheduler를 여러 서버에서 실행하면 ​중복 실행이라는 문제가 발생하며, 사용자가 증가하고 데이터가 계속 변화하면서 처음에는 보이지 않았던 문제들이 나타날 수도 있다.

결국 이번 프로젝트를 통해 가장 크게 배운 것은 특정 기술이나 라이브러리 자체보다 "시스템은 내가 작성한 코드대로만 움직이지 않는다"​는 사실이다.

그리고 좋은 백엔드 개발자는 단순히 Happy Path를 구현하는 사람이 아니라,

- 데이터가 예상과 다르게 변하면 어떻게 되는가?
- 여러 요청이 동시에 들어오면 어떻게 되는가?
- 서버가 여러 대라면 어떻게 되는가?
- 외부 시스템이 실패하면 어떻게 되는가?
- 캐시와 DB의 데이터가 달라지면 어떻게 되는가?
- 실제 사용자가 늘어나고 데이터가 계속 쌓이면 어떻게 되는가?
- 내가 생각하지 못한 입력이 들어오면 어떻게 되는가?

와 같은 질문을 끊임없이 던지는 사람에 가깝다고 생각하게 되었다.

앞으로는 단순히 기능을 만드는 개발자가 아니라 '실패하면 어떻게 되는지', '그래서 나는 무엇을 해야 하는지', '데이터가 시간이 흐름에 따라 변화하면 내 로직은 어떻게 되는지'를 처음부터 고려하는 개발자가 되고자 한다.




























